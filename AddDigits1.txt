public class Solution {
    public int addDigits(int num) {
        int res=0;int sum=0;
        while(num!=0)
        {
            res=num%10;
            sum+=res;
            num=num/10;
        }
        if(sum<10)
        return sum;
        else
        return addDigits(sum);
    }
}