public class Solution {
    public int rangeBitwiseAnd(int m, int n) {
        if(m==0 && n==0)
        return 0;
       int offset=0;
       while(true)
       {
           if(m==n)
           return m<<offset;
           m>>=1;
           n>>=1;
           offset++;
       }
    }
}